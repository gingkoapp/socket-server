{
  "name": "socket-server",
  "version": "0.1.0",
  "repository": "git://github.com:ask11/socket-server.git",
  "scripts": {
    "test": "mocha",
    "stat": "cloc lib client --quiet --by-file && cloc test/*.js --quiet --exclude-dir=support"
  },
  "dependencies": {
    "socket.io": "0.9.16",
    "ua-parser": "~0.3.3",
    "passport.socketio": "~1.2.1"
  },
  "devDependencies": {
    "socket.io-client": "0.9.16",
    "chai": "*",
    "mocha": "*",
    "underscore": "*"
  }
}
